{
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "storybook": "turbo run storybook --parallel",
    "build-tokens": "turbo run build-tokens",
    "panda-codegen": "turbo run panda-codegen",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.11.0"
  },
  "publishConfig": {
    "@b.freguglia:registry": "https://npm.pkg.github.com"
  }
}
